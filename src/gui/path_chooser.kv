<PathChooser>
    orientation: 'vertical'
#    on_drop_file: print("Dropped a file: " + root.file_type)
    MDBoxLayout:
        size_hint_y: 0.5
        MDLabel:
#                size_hint_x: 0.9
            text: "Drag the " + root.file_type + " file or enter it's path"
            font_style: "H5"
    MDBoxLayout:
#            md_bg_color: app.theme_cls.bg_dark
        orientation: 'horizontal'
#            adaptive_height: True
        size_hint_y: 0.5
        padding: dp(10)
        MDTextField:
            id: input_field
            hint_text: root.file_type + " file path"

        MDIconButton:
            icon: 'check-outline'
            on_release: print("buttn clik")#root.add_item(root.ids.input_field.text); root.ids.input_field.text = ""


